---
interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<span class="tag-badge inline-flex h-6 items-center rounded-md border border-kb-border bg-kb-surface-hover px-2 text-xs font-medium text-kb-muted">
  #{tag.replace(/^#+|#+$/g, '')}
</span>
